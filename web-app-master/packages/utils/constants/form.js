export const FieldTypeEnum = {
  INTEGER: 'INTEGER',
  NUMERIC: 'NUMERIC',
  STRING: 'STRING',
  TEXT: 'TEXT',
  DATE_TIME: 'DATE_TIME',
  REFERENCE: 'REFERENCE',
  ENUM: 'ENUM',
}

export const EnumDiscriminatorEnum = {
  BOOLEAN: 'boolean',
  STRING: 'string',
  TIME: 'time',
}

export const FieldRecordDiscriminatorEnum = {
  INTEGER :'integer',
  NUMERIC :'numeric',
  STRING :'string',
  TEXT :'text',
  DATE_TIME :'date_time',
  REFERENCE :'reference',
  STRING_ENUM :'string_enum',
  BOOLEAN_ENUM :'boolean_enum',
  TIME_ENUM :'time_enum',
}

export const FieldVariantTypeEnum = {
  PERCENTAGE: 'PERCENTAGE',
}

export const VisibilityConditionDependentTypeEnum = {
  IS: 'IS',
  IS_NOT: 'IS_NOT',
}

export const VisibilityConditionDiscriminatorEnum = {
  STRING_ENUM: 'string_enum',
  BOOLEAN_ENUM: 'boolean_enum',
}
