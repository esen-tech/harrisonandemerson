import React from 'react'

import Separater from '../Separater'

export default {
  title: 'ĒSEN/Separater',
  component: Separater,
}

const Template = (args) => <Separater {...args} />

export const Primary = Template.bind({})
Primary.args = {}
